# Committed on purpose: loaded only by "vite build --mode private" (npm run build:private).
# Private, local builds only. Never deploy a build made in this mode (SPEC §11, REQ-BRD-02).
VITE_BRAND_MODE=real
